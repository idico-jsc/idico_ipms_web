# Bump version before commit
node scripts/bump-version.js

# Add package.json if version was bumped
git add package.json

# Run lint-staged (temporarily disabled due to prettier permission issue)
# npm run lint-staged --concurrent false
