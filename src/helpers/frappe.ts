// import { FrappeError } from "@/types";


export const getFrappeMessages = () => {
//   if (!error) return null;
//   try {
//     if (error._server_messages) {
//       const parsedMessages: FrappeServerMessage[] = JSON.parse(error._server_messages);
//       return parsedMessages;
//     }
//     if(error
//   } catch (error) {
//     console.error("Error parsing Frappe messages:", error);
//     return [];
//   }
};
