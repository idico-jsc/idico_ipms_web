# Molecules

Molecules are combinations of atoms functioning together as a unit.

## Examples
- Form fields (label + input + error message)
- Search box (input + button)
- Card header (title + icon + button)
- VersionDisplay

## Guidelines
- Combine 2-3 atoms together
- Should have a single responsibility
- Can contain minimal UI logic
- Should be reusable across different contexts
